import React from "react";
import { LucideIcon } from "lucide-react";

// Use Case custom data type
export default interface UseCaseType {
    title: string,
    description: string,
    icon: LucideIcon
}